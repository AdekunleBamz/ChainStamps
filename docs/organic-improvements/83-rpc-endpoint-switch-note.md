# RPC Endpoint Switch

- Add a controlled endpoint switch path for degraded RPC providers.
- Persist the selected endpoint for the active session only.
- This gives operators fast recovery without long-term config drift.
