# Release Vite warning baseline

Keep a short list of expected Vite warnings in release verification so new
warnings from app code are not hidden by known dependency noise.

Follow-up note: Update the baseline whenever dependency warning text changes.
