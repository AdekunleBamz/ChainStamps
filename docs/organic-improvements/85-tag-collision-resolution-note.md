# Tag Collision Resolution

- When duplicate keys are detected, show existing value before overwrite.
- Offer rename and replace as explicit separate actions.
- This prevents unintended metadata replacement.
