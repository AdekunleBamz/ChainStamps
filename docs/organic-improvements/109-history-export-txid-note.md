# History export txid note

Keep transaction IDs in history-export support notes so external audit records can be matched back to on-chain registry writes.

## Checklist

- Export a history sample after hash, stamp, and tag writes.
- Confirm each exported row preserves the transaction ID and registry type.
