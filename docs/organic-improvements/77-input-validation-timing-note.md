# Input Validation Timing

- Validate user inputs on blur for heavy checks and on change for simple checks.
- Avoid full-form validation on every keystroke.
- This keeps feedback responsive without noisy errors.
