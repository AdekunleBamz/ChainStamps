# Readonly mode submit disabled reason

Document copy expectations for submit controls disabled by read-only mode.

The disabled reason should identify the mode rather than implying validation failed.
