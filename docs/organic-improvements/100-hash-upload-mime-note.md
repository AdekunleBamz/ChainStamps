# Hash upload MIME note

Keep uploaded file type context in hash-registry support notes so unexpected digest reports include the browser-provided MIME value.

## Checklist

- Capture filename, size, and MIME type when reproducing upload hashing issues.
- Confirm the same file hashes consistently after renaming.
