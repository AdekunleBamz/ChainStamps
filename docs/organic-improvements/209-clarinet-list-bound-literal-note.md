# Clarinet list bound literal note

For `as-max-len?` checks, keep a focused Clarinet run after replacing literal list bounds with constants.

Some Clarinet versions require the bound argument to remain a literal value.

If a constant is kept for documentation, confirm the compiled call still uses the accepted literal bound.
