# Registry filter count label

Add a dashboard note for labeling result counts after registry filters are applied.

The count should identify whether it reflects loaded rows or the full matching set.
