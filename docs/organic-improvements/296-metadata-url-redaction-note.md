# Metadata URL redaction note

Support notes should include metadata host names for debugging but redact full
paths when they could reveal private customer file names.

Follow-up note: Recheck redaction examples after metadata preview logging changes.
