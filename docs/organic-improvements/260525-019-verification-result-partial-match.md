# Verification result partial match

Track copy for proof verification when the hash matches but metadata differs.

The result should make the partial match explicit and suggest which field to inspect.
