# Network Latency Status

- Show a lightweight network latency indicator in diagnostic panel.
- Update status at fixed intervals without noisy animation.
- This gives context for slow read/write operations.
