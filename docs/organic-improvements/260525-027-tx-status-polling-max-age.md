# Transaction status polling max age

Add a UX note for showing when transaction status polling has exceeded the expected wait window.

The state should offer a manual explorer check before asking users to retry.
