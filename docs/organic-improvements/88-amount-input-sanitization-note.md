# Amount Input Sanitization

- Reject scientific notation and non-decimal separators in amount fields.
- Normalize valid decimal input before fee calculation.
- This prevents ambiguous amount interpretation.
