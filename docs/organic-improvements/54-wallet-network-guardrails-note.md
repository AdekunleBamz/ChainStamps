# Wallet Network Guardrails

- Block write actions when wallet network differs from configured environment.
- Show the expected network name inline near action buttons.
- This prevents accidental submissions on the wrong chain.
