# Export CSV Field Order

- Keep exported stamp fields in a stable order across releases.
- Place identifiers and network values before user-facing descriptions.
- Include field order checks in release handoff review.
