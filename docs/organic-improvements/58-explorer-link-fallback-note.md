# Explorer Link Fallback

- Provide a fallback explorer link when the primary endpoint is unavailable.
- Keep transaction hashes copyable even if explorer pages cannot load.
- This preserves verification workflows during explorer outages.
- Recheck fallback hosts after network configuration changes.
