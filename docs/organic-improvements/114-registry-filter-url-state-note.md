# Registry filter URL state note

Keep registry filter URL state in QA notes so shared links preserve the selected record type and search query.

## Checklist

- Share a filtered registry view in a clean browser profile.
- Confirm the destination page restores the same filter and query.
