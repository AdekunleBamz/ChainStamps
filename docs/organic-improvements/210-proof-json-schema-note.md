# Proof JSON schema

When proof downloads change, keep a small schema note beside the release so SDK
consumers know which fields are stable and which are display-only.

Follow-up note: Recheck schema examples for network and contract alias fields.
