# Tag Key Normalization

- Normalize tag keys to a consistent case before write operations.
- Display normalized key in confirmation step.
- This prevents duplicate keys that differ only by letter casing.
