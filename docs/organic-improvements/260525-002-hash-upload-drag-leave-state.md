# Hash upload drag leave state

Document a small drag-and-drop check for clearing upload hover state after the pointer leaves the drop zone.

This prevents the uploader from looking active when no file is being dropped.
