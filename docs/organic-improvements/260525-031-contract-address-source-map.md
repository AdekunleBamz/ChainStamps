# Contract address source map

Add a release note for mapping contract addresses back to deployment plans and explorer pages.

The map helps reviewers verify frontend environment values without guessing.
