# File hash large file note

Keep large-file hashing in browser smoke checks so progress and memory behavior remain predictable for real evidence uploads.

## Checklist

- Hash at least one large file in a throttled browser profile.
- Confirm the UI stays responsive until the digest is ready.
