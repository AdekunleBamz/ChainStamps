# Tag value preview redaction

Keep tag value preview redaction in review notes so screenshots do not expose private labels while still showing enough context for debugging.

## Checklist

- Blur sensitive tag values before sharing UI captures.
- Preserve tag key, network, and transaction ID in the support bundle.
